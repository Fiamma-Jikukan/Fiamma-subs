This project uses FFmpeg commands and bash scripts to alter and burn subtitles in .ass format into a video.

1. It separates existing input video into empty video (no subtitles) and subtitles file.

2. It alters the subtitles to the desired format

3. It burns the new subtitles into the empty video.

Entry point: fiamma_subing.sh
